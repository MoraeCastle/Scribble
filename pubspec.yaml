name: scribble
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  package_rename: ^1.5.3
  flutter_native_splash: ^2.3.11
  flutter_launcher_icons: ^0.13.1
  keyboard_service: ^2.0.2
  url_launcher: ^6.2.5
  quill_html_editor: ^2.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
